server:
  port: 6666

invoicing-system:
  database: file
  database.directory: db
  database.invoices.file: invoices.txt
  database.id.file: id.txt

logging:
  level:
    pl:
      futurecollars:
        invoicing: TRACE
        invoicing.db: DEBUG